# Prefer: pip install -e ".[dev]" from the repo root (see pyproject.toml).
mcp>=1.0.0
openai>=1.30.0
numpy>=1.26.0
pydantic>=2.5.0
uvicorn>=0.30.0
starlette>=0.38.0
psycopg[binary,pool]>=3.2
httpx>=0.27.0
